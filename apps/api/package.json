{
  "name": "@daily-timeline/api",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc -p tsconfig.json",
    "start": "node dist/server.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@daily-timeline/types": "0.1.0",
    "fastify": "^5.2.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "vitest": "^3.0.2",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2",
    "@types/node": "^25.5.2"
  }
}
